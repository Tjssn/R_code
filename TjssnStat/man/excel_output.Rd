% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excel_output.R
\name{excel_output}
\alias{excel_output}
\title{Export Analysis Results to Excel}
\usage{
excel_output(Object, Path = NULL, name = NULL)
}
\arguments{
\item{Object}{A \code{super_param} object. Must contain \code{output$result$excel_output$body} (the data to export).}

\item{Path}{Character string. Path to the directory where the Excel file will be saved. Defaults to the current working directory.}

\item{name}{Character string. Name of the Excel file (with or without \code{.xlsx} extension). Defaults to:
\itemize{
\item \code{Object$output$result$excel_output$file_name} (if available),
\item \code{default_output.xlsx} (if no name is specified).
}}
}
\value{
Invisibly returns the full path to the generated Excel file (character string). Returns \code{NULL} if export fails.
}
\description{
This function exports structured results from a \code{super_param} object (stored in \code{output$result$excel_output$body})
to an Excel file. It supports custom file paths and names, with automatic handling of existing files (e.g., appending timestamps to avoid overwrites).
}
\details{
The function performs the following steps:
\enumerate{
\item Determines the output directory (\code{Path}): uses the current working directory if not specified.
\item Generates the output file name (\code{name}):
\itemize{
\item Uses \code{Object$output$result$excel_output$file_name} if available,
\item Falls back to \code{default_output.xlsx} if no name is provided.
}
\item Checks for existing files and handles conflicts (asks user to overwrite/rename/abort in interactive mode).
\item Creates the output directory if it doesn't exist.
\item Writes data from \code{Object$output$result$excel_output$body} to the Excel file using \code{openxlsx}.
}
}
\note{
\itemize{
\item Requires the \code{openxlsx} package. Install it with \code{install.packages("openxlsx")} if missing.
\item The input \code{Object} must contain \code{output$result$excel_output$body} (a list of data frames, where each element becomes a sheet in Excel).
\item If the output file already exists:
\itemize{
\item In interactive mode (e.g., RStudio), users are prompted to overwrite, rename (with a timestamp), or abort.
\item In non-interactive mode, the file is automatically renamed with a timestamp (\code{YYYYMMDD_HHMMSS}).
}
\item Timestamps use the system's local time.
}
}
\seealso{
\itemize{
\item \code{excel_in}: Import configuration data from Excel.
\item \code{super_param}: Generate the \code{super_param} object containing results to export.
}
}
\author{
TjssnStat Development Team \href{mailto:tongjibbb@163.com}{tongjibbb@163.com}
}
\keyword{Excel}
\keyword{data-writing}
\keyword{export}
\keyword{results}
